// const router = require("express").Router();
// const validate = require("../middlewares/validator");
// // const authorization = require("../middlewares/authenticator");
// const idChecker = require("../middlewares/idChecker");
// const { createValidator, updateValidator } = require("../validations/Blogs");

// const Controller = require("../controllers/Blogs");

// router.get("/", Controller.index);
// router.get("/:id", idChecker(), Controller.findById);
// router.post("/", validate(createValidator), Controller.create);
// router.put("/:id", idChecker(), validate(updateValidator), Controller.update);
// router.delete("/:id", idChecker(), Controller.delete);


// module.exports = router;
